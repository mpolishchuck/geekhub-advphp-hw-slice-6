<div class="panel panel-default">
    <div class="panel-heading">{{ title }}</div>
    <div class="panel-body">
        <ul>
            {% for article in articles %}
                <li><a href="{{ url('paul_maxwell_blog_article', {'id': article.id}) }}">{{ article.title }}</a></li>
            {% endfor %}
        </ul>
    </div>
</div>
